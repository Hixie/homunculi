[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "modelrelay"
version = "0.6.0"
requires-python = ">=3.11"
dependencies = [
    "websockets>=12",
]
